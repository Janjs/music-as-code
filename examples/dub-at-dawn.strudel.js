// Sparse dub techno at 112 BPM, with one 4/4 bar per cycle.
stack(
  s("bd ~ [~ bd] ~, ~ sd ~ sd, hh*8")
    .bank("RolandTR909")
    .gain("0.72 0.42 0.56 0.42")
    .room(0.12),
  note("<a1 a1 g1 e1>")
    .s("sawtooth")
    .ply(4)
    .clip(0.45)
    .lpf(perlin.range(280, 720).slow(8))
    .lpq(5)
    .gain(0.34),
  chord("<Am7 F^7 C^7 G6>")
    .voicing()
    .s("gm_epiano1")
    .attack(0.02)
    .release(0.7)
    .lpf(1800)
    .gain(0.24)
    .delay(0.28)
    .delaytime(0.375)
    .delayfeedback(0.32)
    .room(0.35)
).cpm(28)
